@page
@model ExternalLoginModel
@{
    ViewData["Title"] = "Đăng ký tài khoản";
}

<h1>@ViewData["Title"]</h1>
<h2 id="external-login-title">Đăng ký tài khoản với @Model.ProviderDisplayName account của bạn.</h2>
<hr />

<p id="external-login-description" class="text-info">
    Bạn đã xác thực tài khoản Google thành công, nhấn nút "Đăng ký" để tiếp tục.
    @* You've successfully authenticated with <strong>@Model.ProviderDisplayName</strong>.
    Please enter an email address for this site below and click the Register button to finish
    logging in. *@
    <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-floating">
            <input asp-for="Input.Email" class="form-control" autocomplete="email" readonly hidden />
            <label asp-for="Input.Email" class="form-label" hidden></label>
            <span asp-validation-for="Input.Email" class="text-danger"></span>
        </div>
        <button type="submit" class="btn btn-lg btn-primary">Đăng ký</button>
    </form>
</p>
<br />
<p id="external-login-description" class="text-info">
    <div>Nhấn vào đây nếu chưa nhận được Email xác thực: </div>
    <form asp-page-handler="ReSendEmail">
        <div class="form-floating">
            <input asp-for="Input.Email" class="form-control" autocomplete="email" readonly hidden />
        </div>
        <button type="submit" class="btn btn-lg btn-primary">Gửi lại email xác thực</button>
    </form>
</p>
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<br />

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
